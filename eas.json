{
  "cli": {
    "version": ">= 12.6.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "buildType": "apk" // Add this line to specify APK build type
    },
    "preview": {
      "distribution": "internal",
      "buildType": "apk" // Add this line to specify APK build type
    },
    "production": {
      "autoIncrement": true,
      "buildType": "apk" // Add this line to specify APK build type
    }
  },
  "submit": {
    "production": {}
  }
}
